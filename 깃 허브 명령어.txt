깃 허브 명령여

git add . 
스테이지에 올리는거
git status
스테이지에 올라가있는 현황 보는거
git commit -m "eastar93"
eastar93이라는 이름으로 커밋작업
git push origin master
깃 서버에 올리는 작업

git clone "주소" .
서버에 올라와있는것을 로컬쪽에 복제
git pull "주소"
서버에 올라와있는것을 서버 복제해놓은곳에 끌어다쓰기